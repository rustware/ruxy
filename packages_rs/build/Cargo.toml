[package]
name = "ruxy_build"
version = "0.1.0"
edition = "2024"

[dependencies]
ruxy_util = { path = "../util" }
ruxy_config = { path = "../config" }
ruxy_routing = { path = "../routing" }

quote.workspace = true
proc-macro2.workspace = true

[lints]
workspace = true
